export interface TrustBadge {
  icon: any;
  text: string;
}

export interface StatCard {
  icon?: any;
  label: string;
  numericValue: number;
  suffix: string;
  sub: string;
  rotation: string;
  color: string;
}
